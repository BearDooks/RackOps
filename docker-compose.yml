version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite:///./dcim.db
      # Example for PostgreSQL:
      # - DATABASE_URL=postgresql://user:password@db:5432/dcim

